/**
 * Next.js 설정 파일.
 *
 * 여기에 빌드/런타임 옵션을 넣는다.
 * 예: 이미지 도메인 허용, 리다이렉트, standalone 출력 등
 *
 * 지금은 기본값만 두고, 배포/기능 필요하면 옵션을 추가한다.
 */

// NextConfig = Next.js 설정 객체의 타입
import type { NextConfig } from "next";

// 설정 객체 (현재는 비어 있음 = 기본 동작)
const nextConfig: NextConfig = {
  /* config options here */
};

// Next.js가 이 값을 읽어서 설정을 적용
export default nextConfig;
